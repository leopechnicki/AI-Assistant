# Docker Compose environment for Hex + Ollama
# Copy to .env.docker and fill in your values.

# Gateway auth token (keep secret)
HEX_GATEWAY_TOKEN=changeme

# Gateway port (default: 18789)
HEX_GATEWAY_PORT=18789

# Telegram bot token (get from @BotFather)
TELEGRAM_BOT_TOKEN=
